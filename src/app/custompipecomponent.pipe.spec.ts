import { CustompipecomponentPipe } from './custompipecomponent.pipe';

describe('CustompipecomponentPipe', () => {
  it('create an instance', () => {
    const pipe = new CustompipecomponentPipe();
    expect(pipe).toBeTruthy();
  });
});
